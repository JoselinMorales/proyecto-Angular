import { DecoradorhashPipe } from './decoradorhash.pipe';

describe('DecoradorhashPipe', () => {
  it('create an instance', () => {
    const pipe = new DecoradorhashPipe();
    expect(pipe).toBeTruthy();
  });
});
